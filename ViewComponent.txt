ViewComponents dosyasında ki CategoryVC ürettik;

=>
  public IViewComponentResult Invoke()
        {
            var model = _categoryRepository.List();
            return View(model);
        }

ve

Views > Shared > Components altına aynı sınıf ısmınde bir dosya açtık ve Default.cs oluşturduk . Buraya istediğin template i yapıstır.